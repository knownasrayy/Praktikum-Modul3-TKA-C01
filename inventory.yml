all:
  children:
    backend_nodes:
      hosts:
        node_backend:
          ansible_host: BACKEND_HOST
          ansible_port: 22
          ansible_user: ubuntu
    frontend_nodes:
      hosts:
        node_frontend:
          ansible_host: FRONTEND_HOST
          ansible_port: 22
          ansible_user: ubuntu