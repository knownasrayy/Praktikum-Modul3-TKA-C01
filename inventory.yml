all:
  children:
    backend_nodes:
      hosts:
        node_backend:
          ansible_host: localhost # Ganti dengan IP VM Backend
          ansible_user: ubuntu
          ansible_connection: local
    frontend_nodes:
      hosts:
        node_frontend:
          ansible_host: localhost # Ganti dengan IP VM Frontend
          ansible_user: ubuntu
          ansible_connection: local
